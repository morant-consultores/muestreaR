% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mapa_interactivo.R
\name{mapa_interactivo_seccional}
\alias{mapa_interactivo_seccional}
\title{Mapa interactivo de la muestra seccional para planear rutas (leaflet)}
\usage{
mapa_interactivo_seccional(
  diseño,
  cartografia,
  ruta = NULL,
  secciones = NULL,
  archivo = NULL,
  titulo = "Muestra seccional — planeación de rutas"
)
}
\arguments{
\item{diseño}{Objeto \link{DiseñoINE} con la muestra extraída.}

\item{cartografia}{Objeto \link{CartografiaINE}/\link{Cartografia} (o su lista
\verb{$shp}) con capas MANZANA/SECCION/MUNICIPIO (o MZA/SECC/MUN).}

\item{ruta}{\code{data.frame} opcional del presupuesto de campo por manzana,
keyed por \code{cluster_0}: \code{orden_ruta}, \code{toques_esperados},
\code{dentro_presupuesto}, \code{puertas_presupuesto_seccion} (el listado del
presupuesto). Sin él, se muestran las viviendas n_0 del modelo.}

\item{archivo}{Ruta opcional del HTML a exportar (autocontenido si hay
pandoc). \code{NULL} devuelve el objeto leaflet.}

\item{titulo}{Título del mapa.}
}
\value{
Un objeto \code{leaflet}, o la ruta del HTML si se dio \code{archivo}.
}
\description{
El equivalente de \code{\link[=mapa_interactivo_ageb]{mapa_interactivo_ageb()}} para el flujo INE por
SECCIÓN electoral (\code{\link[=disenar_muestra_ine]{disenar_muestra_ine()}}): un solo HTML con todas
las manzanas sorteadas coloreadas por sección y numeradas con su ORDEN
DE RUTA (el mismo del listado/Excel de campo), el contorno de las
secciones con su presupuesto, y la capa municipal de cobertura. Capas
base calles/satélite, control de capas, leyenda y medidor de
distancias.
}
\seealso{
\code{\link[=mapa_interactivo_ageb]{mapa_interactivo_ageb()}}, \code{\link[=disenar_muestra_ine]{disenar_muestra_ine()}}
}
