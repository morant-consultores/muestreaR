% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marco_ageb.R
\name{reconciliar_marco_cartografia}
\alias{reconciliar_marco_cartografia}
\title{Reconciliar el marco censal con la cartografía (excluir lo no mapeable)}
\usage{
reconciliar_marco_cartografia(
  marco,
  cartografia,
  llave_ageb = "AULR",
  llave_manzana = "MZA",
  variable_tamano = "P_18YMAS"
)
}
\arguments{
\item{marco}{Marco por manzana de \code{\link[=crear_mm_ageb]{crear_mm_ageb()}} (con \code{AULR} y \code{MZA}).}

\item{cartografia}{Objeto \link{Cartografia} (o su lista \verb{$shp}) con las capas
\code{AGEB} (polígonos de AGEB urbana, llave \code{AULR}) y \code{MZA} (polígonos de
manzana, llave \code{MZA}).}

\item{llave_ageb, llave_manzana}{Columnas llave de AGEB y manzana.}

\item{variable_tamano}{Medida de tamaño para reportar el \% de cobertura
excluido (default \code{"P_18YMAS"}).}
}
\value{
El marco filtrado, con el atributo \code{"cobertura"}: tibble de las
manzanas excluidas (\code{MZA}, \code{AGEB}, \code{motivo} — \code{"AGEB sin polígono"} o
\code{"manzana sin polígono"}).
}
\description{
Deja en el marco SOLO las manzanas con información \strong{completa}: censo
(ya lo trae \code{\link[=crear_mm_ageb]{crear_mm_ageb()}}) más \strong{polígono de manzana} y \strong{polígono
de su AGEB} en la cartografía. Una manzana o un AGEB sin polígono no se
puede localizar ni mapear en campo, así que \strong{no debe ser muestreable}:
se excluye del marco ANTES del sorteo (mismo principio que
\code{\link[=aplicar_lista_negra]{aplicar_lista_negra()}} — la exclusión se declara, no se resuelve en
silencio). Es la garantía de que todo lo que se sortea tiene con qué
levantarse.
}
\details{
El desajuste típico: el censo (2020) y el marco geoestadístico de
shapefiles (p. ej. 2025) no coinciden 1 a 1 porque el INEGI fusiona y
divide AGEBs entre años; los AGEBs del censo sin polígono vigente quedan
fuera y se reportan como cobertura.
}
\seealso{
\code{\link[=crear_mm_ageb]{crear_mm_ageb()}}, \code{\link[=disenar_muestra_ageb]{disenar_muestra_ageb()}}
}
