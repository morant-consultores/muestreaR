% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mapas.R
\name{clonar_diseno}
\alias{clonar_diseno}
\title{Clonar un diseño conservando sus atributos}
\usage{
clonar_diseno(diseño, deep = TRUE)
}
\arguments{
\item{diseño}{Objeto de diseño (R6) a clonar.}

\item{deep}{¿Clonado profundo? (default \code{TRUE}, que es lo que se quiere cuando
se va a modificar la muestra del clon sin tocar el original).}
}
\value{
El clon, con los atributos del original.
}
\description{
\verb{$clone()} de R6 copia los campos del objeto pero \strong{no sus atributos}, y
muestreaR guarda en atributos cosas que el diseño necesita para describirse:
\code{asignacion} (de donde sale la tasa de rechazo y la dosis), \code{numeracion_base},
\code{tasas_cluster} y \code{plan_ageb}. Un clon los pierde en silencio.
}
\details{
El síntoma típico es un mapa impreso que promete una entrevista por puerta:
sin \code{asignacion}, \code{\link[=resumen_operativo]{resumen_operativo()}} no puede aplicar la tasa de rechazo.
}
\examples{
\dontrun{
d2 <- clonar_diseno(diseno)          # conserva la asignación
identical(attr(d2, "asignacion"), attr(diseno, "asignacion"))  # TRUE
}
}
