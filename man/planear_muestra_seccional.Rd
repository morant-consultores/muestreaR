% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plan_seccional.R
\name{planear_muestra_seccional}
\alias{planear_muestra_seccional}
\title{Planear una muestra seccional: PPS por estrato con plan versionado}
\usage{
planear_muestra_seccional(
  marco,
  n_total,
  m_por_seccion = 8,
  potencia = 0.5,
  dominio = "region",
  variable_estrato = "estrato",
  variable_tamano = "lista_nominal",
  llave_seccion = "seccion",
  min_secciones = 2,
  tasa_rechazo = 0,
  lista_negra = NULL,
  semilla = NULL
)
}
\arguments{
\item{marco}{Marco seccional estratificado (ver \code{\link[=estratificar_electoral]{estratificar_electoral()}}).}

\item{n_total}{Entrevistas efectivas objetivo en total.}

\item{m_por_seccion}{Entrevistas planeadas por sección (6–8 recomendado:
controla el deff de conglomerado \verb{1 + (m-1)·rho}).}

\item{potencia}{Exponente de la asignación por dominio, en \verb{[0, 1]}.}

\item{dominio}{Columna del dominio de interés (default \code{"region"}). \code{NULL}
aplica la potencia directamente sobre los estratos.}

\item{variable_estrato, variable_tamano}{Columnas del estrato y del tamaño
(lista nominal).}

\item{llave_seccion}{Columna que identifica la sección.}

\item{min_secciones}{Mínimo de secciones por estrato (default 2: con una
sola sección la varianza del estrato no es estimable).}

\item{tasa_rechazo}{Tasa esperada de no respuesta en \verb{[0, 1)}: infla los
\code{contactos} por sección (\code{n_plan / (1 - tasa)}), nunca el \code{n_plan}.}

\item{lista_negra}{Lista opcional \code{list(secciones =, municipios =)} que se
aplica con \code{\link[=aplicar_lista_negra]{aplicar_lista_negra()}} antes del sorteo.}

\item{semilla}{Semilla para reproducibilidad del sorteo.}
}
\value{
\code{tibble} (una fila por sección sorteada) con \code{seccion}, el
dominio, \code{estrato}, \code{ln_seccion}, \code{pi_seccion}, \code{n_plan} (efectivas
planeadas) y \code{contactos} (viviendas a tocar). Atributos: \code{"asignacion"}
(tabla de \code{\link[=asignar_potencia]{asignar_potencia()}}), \code{"dominios"}, \code{"lista_negra"},
\code{"parametros"}.
}
\description{
Sortea las secciones de cada estrato con probabilidad proporcional a la
lista nominal (\code{\link[=seleccionar_pps]{seleccionar_pps()}}, probabilidades de inclusión exactas)
y entrega el \strong{plan muestral versionable}: el contrato de la capa 1 de
pesos de \code{encuestar::construir_diseno_capas()}. Las \code{pi_seccion} se
calculan sobre el marco COMPLETO del estrato (no sobre las sorteadas):
son las probabilidades del diseño, no una renormalización a posteriori.
}
\details{
El plan debe guardarse en el repo de la ola ANTES de campo
(\code{saveRDS(plan, "salidas/plan_ola1.rds")}): sin plan versionado la capa
de ajuste por sección no puede reconstruir los pesos.
}
