% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocesamiento.R
\name{leer_cartografia_inegi}
\alias{leer_cartografia_inegi}
\title{Cargar la cartografía del marco geoestadístico INEGI}
\usage{
leer_cartografia_inegi(carpeta, patron, encoding = "CP1252")
}
\arguments{
\item{carpeta}{Ruta a la carpeta que contiene las subcarpetas de cada
capa (p. ej. \code{"data-raw"}).}

\item{patron}{Prefijo de los archivos (p. ej. \code{"2025_1_15"}).}

\item{encoding}{Codificación de los shapefiles (default \code{"CP1252"},
Windows-1252, la del marco geoestadístico del INEGI — su \code{.cpg} dice
\code{1252}; los nombres con acento se transcodifican a UTF-8 al leer).}
}
\value{
Lista con \code{mun}, \code{loc} (L), \code{agebR} (AR), \code{agebU} (A), \code{lpr}
(LPR) y \code{mza} (M), lista para
\code{Cartografia$new(mun_shp = cart$mun, loc_shp = cart$loc, ...)}.
}
\description{
Lee los seis shapefiles del marco geoestadístico integrado que usa el
flujo censal por AGEB y los devuelve como lista de objetos \code{sf} en CRS
4326, en el orden que espera \code{Cartografia$new()} / \code{\link[=crear_shp]{crear_shp()}}:
municipios, localidades urbanas, AGEB rural, AGEB urbana, localidades
puntuales rurales y manzanas. Los archivos siguen la nomenclatura del
INEGI \verb{\{patron\}_\{CAPA\}} (p. ej. \verb{2025_1_15_A} = AGEB urbana de la
entidad 15, versión 2025.1).
}
