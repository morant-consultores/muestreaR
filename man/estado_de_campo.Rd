% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estado_campo.R
\name{estado_de_campo}
\alias{estado_de_campo}
\title{Estado de campo por manzana y por cluster}
\usage{
estado_de_campo(toques, ruta, reserva, cuotas, n_0 = 6, tope_cierre = 1.25)
}
\arguments{
\item{toques}{Data frame con una fila por intento de puerta:
\code{cluster_2}, \code{manzana_num}, \code{resultado} (\code{"efectiva"}, \code{"rechazo"},
\code{"no_abrio"}, \code{"sin_registro"}) y \code{clase_razon} (\code{"no_vivienda"},
\code{"sin_acceso"}, \code{"recorrida_sin_entrevistas"} o \code{NA}). Puede traer
\code{registro_efectivo} (lógico): \code{TRUE} si el registro de ese \code{SbjNum}
llegó completo, no solo la puerta. Es OPCIONAL por compatibilidad: si
falta, se asume \code{TRUE} para todos (el comportamiento de antes) y se
avisa con un \code{warning}, porque sin ella las efectivas pueden salir
sobrecontadas.}

\item{ruta}{Ruta impresa: \code{cluster}, \code{orden_ruta}, \code{manzana},
\code{seccion}, \code{hoja}, \code{puertas_a_tocar},
\code{presupuesto_puertas_cluster}, \code{entrevistas_meta_cluster}.}

\item{reserva}{Reserva sorteada y NO impresa: \code{cluster},
\code{orden_ruta}, \code{manzana}, \code{seccion}, \code{puertas_esperadas_manzana}.}

\item{cuotas}{Cuotas por cluster: \code{cluster_2}, \code{SECCION},
\code{entrevistas}, \code{puertas}. Se coteja contra \code{ruta} (ver la descripción
de arriba); si algún cluster difiere o falta de un lado,
\code{estado_de_campo()} se detiene con un error que nombra el o los
clusters y ambos valores.}

\item{n_0}{Entrevistas objetivo por manzana (default 6).}

\item{tope_cierre}{Múltiplo de \code{puertas_a_tocar} a partir del cual la
manzana se considera cerrada por presupuesto (default 1.25). Sin
tope, un cluster se lleva varias veces su dosis.}
}
\value{
\code{list} con dos tibbles:
\itemize{
\item \code{manzanas}: una fila por manzana del sorteo (ruta + reserva) más las
manzanas tocadas fuera del sorteo, con su \code{estado}, la \code{accion} de
campo (\code{"CONTINUAR"}, \code{"SUSTITUIR con la manzana <n> (reserva)"},
\code{"RESERVA AGOTADA..."}, \code{"CERRADA"} o \code{"FUERA DEL SORTEO..."}),
\code{sustituta}: el \code{manzana_num} de la reserva asignada a esa manzana
problema, o \code{NA} si no aplica (la reserva se agotó o la manzana no
necesita sustituto) -para no tener que parsear el texto de \code{accion}- y
\code{hay_dato_sucio}: \code{TRUE} si algún toque de esa manzana trae
\code{resultado} \code{NA} (redacción de \code{INT_N} fuera de catálogo, o dato
sucio real). Esos toques NO cuentan en \code{efectivas}/\code{rechazos}/
\code{no_abrio} (se descartan con \code{na.rm}, para que uno solo no ponga en
\code{NA} -y de ahí en 0- la manzana entera), pero tampoco desaparecen sin
dejar rastro: esta bandera es ese rastro, y es \code{FALSE} -no \code{NA}- tanto
para una manzana limpia como para una que nadie tocó todavía.
\item \code{clusters}: una fila por cluster de la ruta (ninguno desaparece,
tenga o no toques) con el presupuesto de puertas consumido y
restante, la tasa medida, el avance de la ruta impresa
(\code{mzas_sin_iniciar} + \code{mzas_en_proceso} + \code{mzas_cerradas} +
\code{mzas_a_sustituir} == \code{manzanas_ruta}) y \code{manzanas_dato_sucio}: cuántas
de sus manzanas (ruta + reserva activada) traen \code{hay_dato_sucio}.
}
}
\description{
Antes de calcular nada, exige que \code{ruta} y \code{cuotas} describan el mismo
plan por cluster (misma \code{entrevistas_meta_cluster}/\code{entrevistas} y mismo
\code{presupuesto_puertas_cluster}/\code{puertas}): si divergen, el material que
campo trae impreso y el plan con el que se sortearon las cuotas ya no
son el mismo, y la función se detiene en vez de reportar avance contra
una meta ajena.
}
