% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plan_seccional.R
\name{estratificar_electoral}
\alias{estratificar_electoral}
\title{Estratificar secciones por tipo electoral y región}
\usage{
estratificar_electoral(
  marco,
  mvn = "margen_victoria_neto",
  region = "region",
  cortes = c(0.15, 0.05, -0.05),
  etiquetas = c("Duro oficialista", "Blando", "Competitiva", "Opositor")
)
}
\arguments{
\item{marco}{\code{tibble} del marco seccional; una fila por sección.}

\item{mvn}{Nombre de la columna con el margen de victoria neto
(oficialista − opositor, normalizado).}

\item{region}{Nombre de la columna con la región de interés del cliente.}

\item{cortes}{Vector numérico decreciente de 3 cortes del MVN:
\verb{> cortes[1]} = duro oficialista, \verb{> cortes[2]} = blando,
\verb{>= cortes[3]} = competitiva, \verb{< cortes[3]} = opositor.}

\item{etiquetas}{Etiquetas de los 4 tipos, en el mismo orden.}
}
\value{
El marco con dos columnas nuevas: \code{tipo_electoral} y \code{estrato}
(\code{region / tipo_electoral}). Las secciones sin MVN quedan con estrato
\code{NA} (se excluyen del sorteo con aviso en \code{\link[=planear_muestra_seccional]{planear_muestra_seccional()}}).
}
\description{
Clasifica cada sección por su comportamiento electoral histórico usando
cortes del margen de victoria neto (MVN) y forma el estrato como la
interacción \strong{región de interés × tipo electoral}. Este criterio
minimiza la varianza dentro del estrato y la maximiza entre estratos
(secciones de voto parecido se parecen entre sí más que las de un mismo
ámbito urbano/rural).
}
\examples{
marco <- tibble::tibble(region = "Capital", lista_nominal = 1000,
                        margen_victoria_neto = c(0.2, -0.1))
estratificar_electoral(marco)
}
