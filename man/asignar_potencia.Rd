% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plan_seccional.R
\name{asignar_potencia}
\alias{asignar_potencia}
\title{Asignar entrevistas a estratos por el método de la potencia}
\usage{
asignar_potencia(
  marco,
  n_total,
  m_por_seccion,
  potencia = 0.5,
  dominio = "region",
  variable_estrato = "estrato",
  variable_tamano = "lista_nominal",
  min_secciones = 2
)
}
\arguments{
\item{marco}{Marco seccional estratificado (ver \code{\link[=estratificar_electoral]{estratificar_electoral()}}).}

\item{n_total}{Entrevistas efectivas objetivo en total.}

\item{m_por_seccion}{Entrevistas planeadas por sección (6–8 recomendado:
controla el deff de conglomerado \verb{1 + (m-1)·rho}).}

\item{potencia}{Exponente de la asignación por dominio, en \verb{[0, 1]}.}

\item{dominio}{Columna del dominio de interés (default \code{"region"}). \code{NULL}
aplica la potencia directamente sobre los estratos.}

\item{variable_estrato, variable_tamano}{Columnas del estrato y del tamaño
(lista nominal).}

\item{min_secciones}{Mínimo de secciones por estrato (default 2: con una
sola sección la varianza del estrato no es estimable).}
}
\value{
\code{tibble} con una fila por estrato: dominio, \code{estrato},
\code{ln_estrato}, \code{secciones_disponibles}, \code{entrevistas_obj}, \code{secciones},
\code{entrevistas_plan} (= \code{secciones * m_por_seccion}). La asignación por
dominio va en el atributo \code{"dominios"}.
}
\description{
Reparte \code{n_total} entrevistas entre dominios de interés con la asignación
de potencia sobre el tamaño poblacional (\code{potencia = 1} proporcional,
\code{0.5} raíz-cuadrada, \code{0} igualitaria) y, dentro de cada dominio,
proporcional a la lista nominal de cada estrato. La raíz-cuadrada es el
punto medio recomendado cuando el cliente pide leer los dominios chicos
sin sacrificar demasiado la precisión estatal.
}
