% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/disenar.R
\name{disenar_muestra_ine}
\alias{disenar_muestra_ine}
\title{Diseñar una muestra polietápica de forma declarativa (marco electoral INE)}
\usage{
disenar_muestra_ine(
  poblacion,
  estratos,
  variable_estrato = "region",
  variable_cluster = "SECCION",
  variable_poblacional = "lista_nominal",
  id_unidad = "id",
  n_0 = 5,
  manzanas_por_seccion = 2,
  tasa_rechazo = 0,
  modo_rechazo = c("manzanas", "secciones"),
  semilla = NULL,
  calcular_cuotas = TRUE,
  ajustar_cuotas = TRUE,
  validar = TRUE
)
}
\arguments{
\item{poblacion}{Objeto \code{PoblacionINE} (o equivalente con \verb{$marco_muestral}).}

\item{estratos}{\code{data.frame} de estratos: columnas \code{estrato}, \code{entrevistas} y,
opcionalmente, \code{tasa_rechazo} (ver \code{\link[=calcular_asignacion]{calcular_asignacion()}}).}

\item{variable_estrato}{Columna del marco que define el estrato (nivel 1).}

\item{variable_cluster}{Columna del conglomerado / último nivel.}

\item{variable_poblacional}{Columna de tamaño para el muestreo proporcional.}

\item{id_unidad}{Columna que identifica la unidad mínima (manzana).}

\item{n_0, manzanas_por_seccion, tasa_rechazo, modo_rechazo}{Parámetros del modelo
operativo (ver \code{\link[=calcular_asignacion]{calcular_asignacion()}}).}

\item{semilla}{Semilla para reproducibilidad (ver \link{DiseñoINE}).}

\item{calcular_cuotas}{\code{logical}. Si es \code{TRUE}, calcula las cuotas.}

\item{ajustar_cuotas}{\code{logical}. Se pasa a \code{calcular_cuotas()}.}

\item{validar}{\code{logical}. Si es \code{TRUE}, valida con \code{\link[=validar_estratos]{validar_estratos()}} y
aborta con un mensaje si hay problemas.}
}
\value{
El objeto \link{DiseñoINE} con la muestra extraída (y cuotas), con la
asignación derivada adjunta como atributo \code{"asignacion"}.
}
\description{
Receta de alto nivel que construye una muestra polietápica a partir de una
tabla de estratos y los parámetros del modelo operativo, ejecutando todo el
pipeline en el orden correcto (niveles, plan, fpc, extracción, cuotas) sin que
el usuario tenga que llamar a los métodos de bajo nivel ni recordar el orden.
}
\details{
Soporta \strong{asignación desproporcionada} (distinto número de entrevistas por
estrato) y \strong{ajuste por rechazo} (ver \code{\link[=calcular_asignacion]{calcular_asignacion()}}). Es aditiva:
usa internamente los métodos de \link{DiseñoINE} sin modificarlos.
}
