% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seleccionar_manzanas.R
\name{seleccionar_manzanas}
\alias{seleccionar_manzanas}
\title{Seleccionar manzanas dentro de cada UPM del plan (Etapa II)}
\usage{
seleccionar_manzanas(
  plan,
  marco_manzanas,
  manzanas_por_upm = 2,
  variable_tamano = "pobtot",
  llave_manzana = "manzana",
  unidad = NULL,
  semilla = NULL
)
}
\arguments{
\item{plan}{Plan muestral versionado (una fila por UPM sorteada), con
atributo \code{"unidad"} o \code{unidad} explícita.}

\item{marco_manzanas}{Marco de manzanas (de \code{\link[=construir_marco_manzanas]{construir_marco_manzanas()}}
o equivalente), con la columna de la unidad y la llave de manzana.}

\item{manzanas_por_upm}{Manzanas a seleccionar en cada UPM (default 2,
el modelo operativo).}

\item{variable_tamano}{Medida de tamaño para el PPT (default \code{"pobtot"};
la población total de manzana está menos enmascarada que la adulta).}

\item{llave_manzana}{Columna que identifica la manzana.}

\item{unidad}{Unidad del plan; por default se lee del atributo.}

\item{semilla}{Semilla para reproducibilidad del sorteo.}
}
\value{
\code{tibble} con una fila por manzana seleccionada: las columnas del
marco de manzanas más \code{pi_manzana}, \code{n_plan} y \code{contactos} (repartidos
por manzana; suman lo del plan en cada UPM). Atributos: \code{"unidad"} y
\code{"parametros"}.
}
\description{
Para cada UPM sorteada del plan (\code{\link[=planear_muestra_upm]{planear_muestra_upm()}} o sus
envolturas) selecciona \code{manzanas_por_upm} manzanas con probabilidad
proporcional al tamaño (\code{\link[=seleccionar_pps]{seleccionar_pps()}}) y reparte el \code{n_plan} y los
\code{contactos} de la UPM entre sus manzanas (\code{\link[=repartir_cociente]{repartir_cociente()}}). Las
\code{pi_manzana} se calculan sobre TODAS las manzanas de la UPM (las
probabilidades del diseño, no una renormalización). Es la Etapa II del
diseño por conglomerados censales (p. ej. 2 manzanas por AGEB, espejo
de la nota de Enkoll); la Etapa III (viviendas, sistemático con
arranque aleatorio) es operación de campo dimensionada por el
\code{n_plan}/\code{contactos} por manzana.
}
\details{
Casos límite, siempre declarados:
\itemize{
\item UPM con menos manzanas que las pedidas: entran todas con \code{pi = 1}
(warning agregado; el plan completo de la UPM se reparte entre ellas).
\item UPM con todas sus manzanas sin tamaño (enmascaramiento INEGI \code{*}):
sorteo equiprobable dentro de la UPM (message agregado).
\item UPM del plan sin manzanas en el marco: error con las llaves (un
listado de campo hueco no debe salir en silencio).
}
}
