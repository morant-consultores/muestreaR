% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mapa_interactivo.R
\name{mapa_interactivo_ageb}
\alias{mapa_interactivo_ageb}
\title{Mapa interactivo de la muestra para planear rutas de campo (leaflet)}
\usage{
mapa_interactivo_ageb(
  diseño,
  cartografia,
  archivo = NULL,
  titulo = "Muestra — planeación de rutas"
)
}
\arguments{
\item{diseño}{Objeto \link{Diseño} con la muestra extraída (censal por AGEB).}

\item{cartografia}{Objeto \link{Cartografia} (o su lista \verb{$shp}) del diseño.}

\item{archivo}{Ruta opcional del HTML a exportar. Si se da, escribe el
widget (autocontenido si hay \code{pandoc}; si no, HTML + carpeta de
dependencias) y devuelve la ruta de forma invisible. Si es \code{NULL}
(default) devuelve el objeto \code{leaflet}.}

\item{titulo}{Título del mapa (y del HTML exportado).}
}
\value{
Un objeto \code{leaflet}, o la ruta del HTML si se dio \code{archivo}.
}
\description{
Construye un mapa interactivo con \strong{todas} las manzanas sorteadas y los
AGEBs de la muestra (flujo censal por AGEB, \code{\link[=disenar_muestra_ageb]{disenar_muestra_ageb()}}),
pensado para que campo \strong{planee las rutas} desde el navegador: las
manzanas se colorean por AGEB (las de un mismo AGEB comparten color, se
visitan juntas) y cada una trae en su popup lo que hay que hacer
(municipio, localidad, AGEB, manzana y viviendas a levantar); cada AGEB
trae su resumen operativo y su número de mapa (para cruzarlo con los PNG
impresos). Incluye capas base (calles y satélite), control de capas y una
herramienta de medición de distancias para trazar recorridos.
}
\details{
Complementa a \code{\link[=google_maps]{google_maps()}} (un PNG por AGEB, para levantar en sitio):
este es un solo HTML de planeación.
}
\seealso{
\code{\link[=google_maps]{google_maps()}}
}
