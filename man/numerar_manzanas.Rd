% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mapas.R
\name{numerar_manzanas}
\alias{numerar_manzanas}
\title{Numerar las manzanas de la muestra dentro de cada conglomerado (1, 2, 3, ...)}
\usage{
numerar_manzanas(diseño)
}
\arguments{
\item{diseño}{Objeto \link{Diseño} (o \link{DiseñoINE}) con la muestra extraída.}
}
\value{
\code{tibble} con una fila por manzana sorteada: la columna del
conglomerado de último nivel (p. ej. \code{cluster_2}), \code{cluster_0} (la
llave de unión con el marco/muestra) y \code{manzana_num} (1..k dentro del
conglomerado).
}
\description{
Asigna a cada manzana sorteada un \strong{identificador corto} dentro de su
conglomerado (1..k), que es el que usa campo en el \strong{cuestionario} (la
clave CVEGEO de 16 caracteres es impráctica para capturar). Es la fuente
única de esa numeración: la consumen \code{\link[=google_maps]{google_maps()}} (número impreso en
cada manzana del PNG), \code{\link[=mapa_interactivo_ageb]{mapa_interactivo_ageb()}} (labels/popups) y los
listados CSV que se entregan a campo — así el número es EL MISMO en los
tres materiales.
}
\details{
La numeración es \strong{estable y reproducible}: dentro de cada conglomerado
se ordena por la clave de manzana (\code{MZA} en el flujo censal, \code{MANZANA}
en el electoral; \code{cluster_0} como último recurso), así que regenerar
cualquier material produce los mismos números. Si el diseño trae el attr
\code{"numeracion_base"} (lo fija \code{\link[=ampliar_manzanas_ageb]{ampliar_manzanas_ageb()}}), esos números son
inmutables y las manzanas nuevas continúan la secuencia (max+1, ...).
}
\seealso{
\code{\link[=google_maps]{google_maps()}}, \code{\link[=mapa_interactivo_ageb]{mapa_interactivo_ageb()}}
}
