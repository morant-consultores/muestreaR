% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mapas.R
\name{google_maps_ine}
\alias{google_maps_ine}
\title{Exportar mapas de campo con Google Maps (marco electoral INE)}
\usage{
google_maps_ine(
  diseño,
  shp,
  zoom,
  dir = "Mapas",
  exportar = T,
  cluster = NULL,
  contexto_m = NULL,
  etiquetas_unidad = NULL
)
}
\arguments{
\item{diseño}{Objeto de la clase \link{DiseñoINE} con la muestra extraída.}

\item{shp}{Lista de cartografías electorales del diseño.}

\item{zoom}{Nivel de zoom de Google Maps.}

\item{dir}{Carpeta de destino de los mapas (por defecto \code{"Mapas"}).}

\item{exportar}{\code{logical}. Si es \code{TRUE}, escribe los mapas en disco.}

\item{cluster}{Identificador opcional de un conglomerado para limitar la
generación de mapas a ese conglomerado.}

\item{contexto_m}{Margen en metros alrededor de las manzanas para encuadrar el
mapa. \code{NULL} (default) encuadra el conglomerado completo, que es lo correcto
cuando las manzanas cubren buena parte de él. Ponerlo es lo que mantiene
legible un conglomerado mucho más grande que lo sorteado (ver
\code{bbox_cluster()}).}

\item{etiquetas_unidad}{Vector o lista CON NOMBRES para separar el título del
nombre del archivo: el nombre es el identificador de la unidad (el que nombra
el PNG) y el valor es el texto que se imprime como título. \code{NULL} (default)
imprime \code{"{u_cluster}: {id}"}, el comportamiento anterior. Las unidades sin
entrada caen al default, así que se puede etiquetar solo algunas.}
}
\value{
Invisible. Se ejecuta por su efecto secundario (escribe los mapas).
}
\description{
Versión para el marco del INE: genera y guarda en disco un mapa por unidad
mínima de la muestra usando imágenes de Google Maps. El subtítulo de cada
mapa indica lo operativo del conglomerado (no cuotas): el zoom, las
manzanas a visitar, los contactos (viviendas a levantar) y las entrevistas
efectivas planeadas.
}
