% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marco_ageb.R
\name{crear_mm_ageb}
\alias{crear_mm_ageb}
\title{Construir el marco muestral censal AGEB para la clase \link{Diseño}}
\usage{
crear_mm_ageb(censo)
}
\arguments{
\item{censo}{\code{tibble} del CSV del censo leído como texto
(\code{readr::read_csv(..., col_types = readr::cols(.default = "c"))}).}
}
\value{
\code{tibble} con una fila por manzana, listo para
\code{PoblacionAGEB$new()} / \link{Diseño}.
}
\description{
Versión del marco por manzana COMPATIBLE CON LA CLASE del equipo
(la que se exporta como \code{diseño.rda} y consumen AppAuditoria y
encuestar): una fila por manzana urbana del dataset "ageb_mza_urbana"
con las columnas y llaves de \code{\link[=crear_mm]{crear_mm()}} — \code{ENTIDAD} (2), \code{MUN} (5),
\code{LOC} (9), \code{AGEB} (13), \code{MZA} (16), las llaves de cruce con la
cartografía (\code{ARLU}, \code{AULR}), \code{id} — y TODO el bloque censal numérico
parseado (de \code{POBTOT} en adelante; el enmascaramiento \code{*} queda como
\code{NA}). \code{\link[=cuotas]{cuotas()}} y el rake \code{tipo_encuesta = "inegi"} de encuestar usan
de ahí \verb{P_18A24_*}, \verb{P_18YMAS_*} y \verb{P_60YMAS_*}.
}
\details{
A diferencia de \code{\link[=crear_mm]{crear_mm()}} no requiere la base de localidades (ITER)
ni shapefiles: el dataset es el universo urbano (localidades con AGEB y
manzana), que es el marco del diseño por AGEBs.
}
