% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocesamiento.R
\name{corregir_lista_nominal}
\alias{corregir_lista_nominal}
\title{Corregir la lista nominal por edad y sexo}
\usage{
corregir_lista_nominal(ln, base_ine)
}
\arguments{
\item{ln}{\code{tibble} de lista nominal (de \code{\link[=leer_lista_nominal_ine]{leer_lista_nominal_ine()}}), con las
columnas \code{SECCION}, \verb{LISTA NOMINAL}, \verb{LISTA HOMBRES}, \verb{LISTA MUJERES} y los
conteos por rango de edad y sexo.}

\item{base_ine}{\code{tibble} de la base del INE por localidad, con \code{SECCION} y
columnas \verb{LISTA*} (incluyendo hombres y mujeres).}
}
\value{
\code{tibble} de lista nominal corregida, con \code{SECCION}, \verb{LISTA NOMINAL} y
los conteos corregidos por rango de edad y sexo.
}
\description{
Reconcilia la lista nominal por rango de edad y sexo (por sección) con la base
del INE por localidad, reescalando los conteos por sexo a los totales de la
base. Es el paso de preprocesamiento más delicado; reproduce exactamente la
lógica usada en producción.
}
