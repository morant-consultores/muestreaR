% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocesamiento.R
\name{corregir_lista_nominal}
\alias{corregir_lista_nominal}
\title{Corregir la lista nominal por edad y sexo}
\usage{
corregir_lista_nominal(ln, base_ine)
}
\arguments{
\item{ln}{\code{tibble} de lista nominal (de \code{\link[=leer_lista_nominal_ine]{leer_lista_nominal_ine()}}), con las
columnas \code{SECCION}, \verb{LISTA NOMINAL}, \verb{LISTA HOMBRES}, \verb{LISTA MUJERES} y los
conteos por rango de edad y sexo.}

\item{base_ine}{\code{tibble} de la base del INE por localidad, con \code{SECCION} y las
columnas \code{LISTA_HOMBRES} y \code{LISTA_MUJERES} (con guion bajo); son los totales
por sexo a los que se reescala la lista nominal.}
}
\value{
\code{tibble} de lista nominal corregida, con \code{SECCION}, \verb{LISTA NOMINAL} y
los conteos corregidos por rango de edad y sexo.
}
\description{
Reconcilia la lista nominal por rango de edad y sexo (por sección) con la base
del INE por localidad, reescalando los conteos por sexo a los totales de la
base. Es el paso de preprocesamiento más delicado; reproduce exactamente la
lógica usada en producción.
}
\section{Supuesto}{
 Se asume que cada sección tiene población \strong{mayor que cero
en ambos sexos} en la lista nominal original. Si un sexo suma cero en una
sección, su población de la base no puede repartirse (división por cero) y se
conserva el original — comportamiento heredado del flujo de producción. Con
datos reales del INE este caso es raro.
}

\section{Redondeo}{
 Cada celda (rango de edad × sexo) se reescala de forma
independiente con \code{round(pct * total_nuevo)}, igual que en producción. Por el
redondeo, la suma de las celdas de un sexo puede diferir del total de la base
en ±1 por sección (no hay ajuste de mayor-residuo). Es despreciable frente al
tamaño de la lista nominal, pero si se requiere cuadre exacto por sexo conviene
aplicar un reparto de cociente entero a posteriori.
}

