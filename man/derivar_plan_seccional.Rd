% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plan_seccional.R
\name{derivar_plan_seccional}
\alias{derivar_plan_seccional}
\title{Derivar el plan muestral versionado desde el diseño INE de la clase}
\usage{
derivar_plan_seccional(diseno, asignacion = attr(diseno, "asignacion"))
}
\arguments{
\item{diseno}{Objeto \link{DiseñoINE} con niveles estrato + sección, \code{fpc}
calculado y muestra extraída (típicamente de \code{\link[=disenar_muestra_ine]{disenar_muestra_ine()}}).}

\item{asignacion}{Tabla de \code{\link[=calcular_asignacion]{calcular_asignacion()}}; por default se lee
del atributo \code{"asignacion"} del diseño.}
}
\value{
Plan versionado: \code{tibble} con \code{seccion}, \code{estrato}, \code{ln_seccion}
(tamaño de la sección en la variable poblacional), \code{pi_seccion},
\code{n_plan} (efectivas objetivo por sección) y \code{contactos} (entrevistas a
levantar por sección). Atributos \code{"unidad"} y \code{"parametros"}.
}
\description{
Reconstruye el \strong{plan versionado por sección} (el contrato de la capa 1
de \code{encuestar::construir_diseno_capas()}) desde un \link{DiseñoINE} con la
muestra ya extraída: las \code{pi_seccion} son exactamente las del sorteo
(el \code{fpc} del nivel de sección, calculado sobre el marco completo del
estrato) — un solo sorteo, cero inconsistencia entre el diseño operativo
y el de pesos. Espejo seccional de \code{\link[=derivar_plan_ageb]{derivar_plan_ageb()}}.
}
