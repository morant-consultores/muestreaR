% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ampliar_ageb.R
\name{ampliar_manzanas_ageb}
\alias{ampliar_manzanas_ageb}
\title{Ampliar aditivamente las manzanas por AGEB de un diseño en campo}
\usage{
ampliar_manzanas_ageb(diseño, manzanas_por_ageb, semilla = NULL)
}
\arguments{
\item{diseño}{Objeto \link{Diseño} con la muestra extraída (típicamente el
\code{diseño.rda} exportado que está operando en campo).}

\item{manzanas_por_ageb}{Objetivo TOTAL de manzanas por AGEB (incluidas
las que ya están en campo).}

\item{semilla}{Semilla del sorteo complementario (documentarla: es parte
del plan versionado de la ampliación).}
}
\value{
Un \link{Diseño} nuevo con la muestra ampliada y los atributos
\code{"asignacion"}, \code{"plan_ageb"}, \code{"numeracion_base"} y \code{"ampliacion"}.
}
\description{
Sorteo \strong{complementario} de manzanas dentro de cada AGEB ya sorteado,
para cuando el rendimiento por puerta observado exige más sobremuestra de
la planeada (diseño sin revisita ni sustitución: el colchón son las
manzanas). La restricción que gobierna todo: \strong{la muestra que ya está en
campo no se toca} — las manzanas originales quedan idénticas (misma fila,
mismo \code{cluster_0}) y conservan su número corto impreso en mapas y
cuestionario (\code{\link[=numerar_manzanas]{numerar_manzanas()}} respeta el attr \code{"numeracion_base"}
que esta función fija); las nuevas continúan la numeración (5, 6, ...).
}
\details{
El sorteo complementario es equiprobable dentro del AGEB — el mismo
esquema de la Etapa II de la clase (\code{\link[=muestrear]{muestrear()}}) — excluyendo las
manzanas ya sorteadas: la probabilidad final de cada manzana del AGEB es
k'/N (simétrica), como si se hubieran sorteado k' desde el inicio. Los
AGEBs con menos manzanas disponibles que el objetivo entran completos
(se declara en el attr \code{"ampliacion"}); el faltante se resuelve en el
siguiente top-up o con \code{\link[=planear_siguiente_ola]{planear_siguiente_ola()}}.

Se devuelve un \strong{clon}: el diseño original no se modifica. En el clon se
actualizan \code{n} (total a levantar), \code{n_i} (m/n del último nivel), la
asignación (\code{entrevistas_a_levantar} recalculado; \code{tasa_rechazo} pasa a
ser la tasa de no-logro implícita \code{1 - efectivas/levantar}, con la que
\code{\link[=resumen_operativo]{resumen_operativo()}} imprime las entrevistas planeadas por mapa) y el
plan versionado (\code{pi_ageb} intactas — mismo sorteo de Etapa I —,
\code{contactos} por AGEB = suma real de \code{n_0} de sus manzanas). Si el diseño
traía cuotas, se recalculan sobre la muestra ampliada.
}
\seealso{
\code{\link[=numerar_manzanas]{numerar_manzanas()}}, \code{\link[=derivar_plan_ageb]{derivar_plan_ageb()}},
\code{\link[=planear_siguiente_ola]{planear_siguiente_ola()}}
}
