% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plan_upm.R
\name{planear_muestra_upm}
\alias{planear_muestra_upm}
\title{Planear una muestra por UPM: PPS por estrato con plan versionado}
\usage{
planear_muestra_upm(
  marco,
  n_total,
  m_por_upm,
  potencia = 0.5,
  dominio = "region",
  variable_estrato = "estrato",
  variable_tamano = "lista_nominal",
  llave_upm = "upm",
  unidad = "upm",
  min_secciones = 2,
  tasa_rechazo = 0,
  lista_negra = NULL,
  semilla = NULL
)
}
\arguments{
\item{marco}{Marco seccional estratificado (ver \code{\link[=estratificar_electoral]{estratificar_electoral()}}).}

\item{n_total}{Entrevistas efectivas objetivo en total.}

\item{m_por_upm}{Entrevistas planeadas por UPM.}

\item{potencia}{Exponente de la asignación por dominio, en \verb{[0, 1]}.}

\item{dominio}{Columna del dominio de interés (default \code{"region"}). \code{NULL}
aplica la potencia directamente sobre los estratos.}

\item{variable_estrato, variable_tamano}{Columnas del estrato y del tamaño
(lista nominal en marcos electorales; población adulta en censales).}

\item{llave_upm}{Columna del marco que identifica la UPM.}

\item{unidad}{Nombre de la UPM; nombra las columnas del plan
(\verb{<unidad>}, \verb{ln_<unidad>}, \verb{pi_<unidad>}) y los mensajes.}

\item{min_secciones}{Mínimo de UPMs por estrato (default 2: con una
sola la varianza del estrato no es estimable).}

\item{tasa_rechazo}{Tasa esperada de no respuesta en \verb{[0, 1)}: infla los
\code{contactos} por UPM (\code{n_plan / (1 - tasa)}), nunca el \code{n_plan}.}

\item{lista_negra}{Lista opcional que se aplica con
\code{\link[=aplicar_lista_negra]{aplicar_lista_negra()}} antes del sorteo. Las UPMs a excluir se pasan
en \code{upms} o en el plural de la unidad (\code{secciones}, \code{agebs}, ...);
los municipios, en \code{municipios}.}

\item{semilla}{Semilla para reproducibilidad del sorteo.}
}
\value{
\code{tibble} (una fila por UPM sorteada) con \verb{<unidad>}, el dominio,
\code{estrato}, \verb{ln_<unidad>} (medida de tamaño), \verb{pi_<unidad>}, \code{n_plan}
(efectivas planeadas) y \code{contactos} (viviendas a tocar). Atributos:
\code{"asignacion"} (tabla de \code{\link[=asignar_potencia]{asignar_potencia()}}), \code{"dominios"},
\code{"lista_negra"}, \code{"unidad"} y \code{"parametros"}.
}
\description{
Motor genérico del plan muestral versionable: sortea las UPMs de cada
estrato con probabilidad proporcional al tamaño (\code{\link[=seleccionar_pps]{seleccionar_pps()}},
probabilidades de inclusión exactas) y entrega el plan que es el
contrato de la capa 1 de pesos de \code{encuestar::construir_diseno_capas()}
(para unidades distintas de la sección, ver \code{\link[=plan_para_capas]{plan_para_capas()}}).
Las \code{pi} se calculan sobre el marco COMPLETO del estrato (no sobre las
sorteadas): son las probabilidades del diseño, no una renormalización a
posteriori.
}
\details{
El plan debe guardarse en el repo de la ola ANTES de campo
(\code{saveRDS(plan, "salidas/plan_ola1.rds")}): sin plan versionado la capa
de ajuste por UPM no puede reconstruir los pesos.
}
\seealso{
\code{\link[=planear_muestra_seccional]{planear_muestra_seccional()}}, \code{\link[=planear_muestra_ageb]{planear_muestra_ageb()}},
\code{\link[=planear_siguiente_ola]{planear_siguiente_ola()}}, \code{\link[=plan_para_capas]{plan_para_capas()}}
}
