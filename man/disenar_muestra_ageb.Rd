% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/disenar_ageb.R
\name{disenar_muestra_ageb}
\alias{disenar_muestra_ageb}
\title{Diseñar una muestra polietápica por AGEB de forma declarativa (clase)}
\usage{
disenar_muestra_ageb(
  poblacion,
  estratos,
  variable_estrato = "region",
  variable_cluster = "AGEB",
  variable_poblacional = "P_18YMAS",
  id_unidad = "id",
  n_0 = 5,
  manzanas_por_ageb = 2,
  tasa_rechazo = 0,
  modo_rechazo = c("manzanas", "agebs"),
  semilla = NULL,
  calcular_cuotas = TRUE,
  validar = TRUE
)
}
\arguments{
\item{poblacion}{Objeto \link{PoblacionAGEB} (o equivalente con
\verb{$marco_muestral} de \code{\link[=crear_mm_ageb]{crear_mm_ageb()}}).}

\item{estratos}{\code{data.frame} de estratos: columnas \code{estrato},
\code{entrevistas} (efectivas objetivo) y opcionalmente \code{tasa_rechazo}
(ver \code{\link[=calcular_asignacion]{calcular_asignacion()}}).}

\item{variable_estrato}{Columna del marco que define el estrato
(nivel 1). Para el espejo sin estratificar, una columna constante.}

\item{variable_cluster}{Columna del conglomerado (default \code{"AGEB"}).}

\item{variable_poblacional}{Columna de tamaño para el PPS (default
\code{"P_18YMAS"}).}

\item{id_unidad}{Columna que identifica la unidad mínima (manzana).}

\item{n_0}{Entrevistas por manzana (default 5).}

\item{manzanas_por_ageb}{Manzanas por AGEB base (default 2).}

\item{tasa_rechazo}{Tasa de rechazo global en \verb{[0, 1)}.}

\item{modo_rechazo}{\code{"manzanas"} (default) o \code{"agebs"}.}

\item{semilla}{Semilla para reproducibilidad.}

\item{calcular_cuotas}{\code{logical}. Cuotas censales de edad/sexo por AGEB
(default \code{TRUE}: son las instrucciones que \code{\link[=google_maps]{google_maps()}} imprime en
los mapas de campo; el espejo tipo Enkoll también las usa).}

\item{validar}{\code{logical}. Validar con \code{\link[=validar_estratos]{validar_estratos()}}.}
}
\value{
El objeto \link{Diseño} con la muestra extraída, la asignación como
atributo \code{"asignacion"} y el plan versionado como \code{"plan_ageb"}.
}
\description{
Receta de alto nivel del \strong{flujo de la clase} para el marco censal por
AGEB: construye el \link{Diseño} completo (niveles estrato + AGEB, plan,
fpc, extracción y cuotas censales) a partir de una tabla de estratos y
el modelo operativo, igual que \code{\link[=disenar_muestra_ine]{disenar_muestra_ine()}} para el marco
electoral. El objeto resultante es el que el equipo ya opera: la
población vive adentro, \code{diseno$exportar(cartografia)} escribe
\code{diseño.rda} + \code{shp.rda} + \code{cuotas.csv} + los mapas de Google por AGEB
(instrucciones del encuestador), y es el insumo de AppAuditoria y de
\code{encuestar} (\code{tipo_encuesta = "inegi"}).
}
\details{
El ajuste por rechazo es el del modelo operativo del equipo:
\code{modo_rechazo = "manzanas"} infla las manzanas por AGEB (p. ej. 2 -> 4
con tasa 0.5) manteniendo \code{n_0} entrevistas por manzana y los AGEBs
fijos; \code{"agebs"} infla los AGEBs con las manzanas fijas.

Además adjunta el \strong{plan muestral versionado} (\code{\link[=derivar_plan_ageb]{derivar_plan_ageb()}},
atributo \code{"plan_ageb"}): el contrato de la capa 1 de
\code{encuestar::construir_diseno_capas()}, derivado del MISMO sorteo de la
clase (cero inconsistencia entre el diseño operativo y el de pesos).
}
