% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plan_para_capas.R
\name{plan_para_capas}
\alias{plan_para_capas}
\title{Adaptar un plan por UPM al contrato de encuestar::construir_diseno_capas}
\usage{
plan_para_capas(plan, unidad = NULL)
}
\arguments{
\item{plan}{Plan muestral versionado (de \code{\link[=planear_muestra_upm]{planear_muestra_upm()}} o sus
envolturas).}

\item{unidad}{Unidad del plan; por default se lee del atributo
\code{"unidad"} y, si no existe, se asume \code{"seccion"}.}
}
\value{
El plan con las columnas del contrato seccional (\code{seccion},
\code{pi_seccion} y, si existe la medida de tamaño, \code{ln_seccion}),
conservando los demás atributos y agregando \code{"unidad_original"}.
}
\description{
\code{encuestar::construir_diseno_capas()} (capa 1 = selección planeada)
espera las columnas del plan seccional: \code{seccion}, \code{pi_seccion},
\code{n_plan} y opcionalmente \code{ln_seccion}. Mientras encuestar hable
"seccional", esta función renombra un plan de cualquier unidad
(\code{\link[=planear_muestra_upm]{planear_muestra_upm()}}, \code{\link[=planear_muestra_ageb]{planear_muestra_ageb()}}) a ese contrato:
\code{seccion} pasa a ser la llave de la UPM que sea (p. ej. el CVEGEO del
AGEB). El snapshot debe traer esa misma llave en la columna que se
declara en el argumento \code{seccion} de \code{construir_diseno_capas()}.
}
\details{
Sobre un plan ya seccional (o sin atributo \code{"unidad"}, como los planes
versionados antes de este cambio) es la identidad.
}
