% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marco_ageb.R
\name{construir_marco_ageb}
\alias{construir_marco_ageb}
\title{Construir el marco muestral por AGEB (censo INEGI)}
\usage{
construir_marco_ageb(censo)
}
\arguments{
\item{censo}{\code{tibble} del CSV del censo leído como texto
(\code{readr::read_csv(..., col_types = readr::cols(.default = "c"))}).}
}
\value{
\code{tibble} con \code{ageb} (llave de 13), \code{entidad}, \code{municipio_cod}
(5), \code{nombre_municipio}, \code{localidad_cod} (9), \code{pob18} (población de
18 años y más, la medida de tamaño default del sorteo), \code{pobtot} y
\code{viviendas} (particulares habitadas). Enmascarados INEGI (\code{*}) quedan
como \code{NA} (tamaño 0 en el sorteo: nunca sorteables).
}
\description{
Deriva el marco de AGEBs urbanas del dataset "ageb_mza_urbana" del
Censo 2020: una fila por AGEB (las filas \code{NOM_LOC == "Total AGEB urbana"}) con su llave CVEGEO de 13 caracteres
(entidad 2 + municipio 3 + localidad 4 + AGEB 4, la misma del shapefile
de AGEB urbana del marco geoestadístico) y sus medidas de tamaño. Es el
insumo de \code{\link[=planear_muestra_ageb]{planear_muestra_ageb()}}.
}
\details{
El marco cubre el universo urbano del censo (localidades con AGEB y
manzana); esa cobertura debe declararse en la nota metodológica.
}
