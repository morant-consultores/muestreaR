% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plan_upm.R
\name{planear_muestra_ageb}
\alias{planear_muestra_ageb}
\title{Planear una muestra por AGEB: PPS por estrato con plan versionado}
\usage{
planear_muestra_ageb(
  marco,
  n_total,
  m_por_ageb = 10,
  potencia = 0.5,
  dominio = "region",
  variable_estrato = "estrato",
  variable_tamano = "pob18",
  llave_ageb = "ageb",
  min_secciones = 2,
  tasa_rechazo = 0,
  lista_negra = NULL,
  semilla = NULL
)
}
\arguments{
\item{marco}{Marco seccional estratificado (ver \code{\link[=estratificar_electoral]{estratificar_electoral()}}).}

\item{n_total}{Entrevistas efectivas objetivo en total.}

\item{m_por_ageb}{Entrevistas planeadas por AGEB (default 10 = 2
manzanas × 5 viviendas, el modelo operativo).}

\item{potencia}{Exponente de la asignación por dominio, en \verb{[0, 1]}.}

\item{dominio}{Columna del dominio de interés (default \code{"region"}). \code{NULL}
aplica la potencia directamente sobre los estratos.}

\item{variable_tamano}{Medida de tamaño para el PPS (default \code{"pob18"}).}

\item{llave_ageb}{Columna del marco que identifica el AGEB (default
\code{"ageb"}, la llave CVEGEO de 13 caracteres de
\code{\link[=construir_marco_ageb]{construir_marco_ageb()}}).}

\item{min_secciones}{Mínimo de UPMs por estrato (default 2: con una
sola la varianza del estrato no es estimable).}

\item{tasa_rechazo}{Tasa esperada de no respuesta en \verb{[0, 1)}: infla los
\code{contactos} por UPM (\code{n_plan / (1 - tasa)}), nunca el \code{n_plan}.}

\item{lista_negra}{Lista opcional \code{list(agebs =, municipios =)} que se
aplica con \code{\link[=aplicar_lista_negra]{aplicar_lista_negra()}} antes del sorteo.}

\item{semilla}{Semilla para reproducibilidad del sorteo.}
}
\value{
Plan muestral por AGEB (mismo esquema que
\code{\link[=planear_muestra_upm]{planear_muestra_upm()}}, con \code{unidad = "ageb"}).
}
\description{
Cara censal de \code{\link[=planear_muestra_upm]{planear_muestra_upm()}}: la UPM es el \strong{AGEB urbano} del
Censo de Población y Vivienda (INEGI) y la medida de tamaño default es
la población de 18 años y más (\code{pob18}, ver \code{\link[=construir_marco_ageb]{construir_marco_ageb()}}).
Emite el plan versionable con columnas \code{ageb}, \code{ln_ageb}, \code{pi_ageb},
\code{n_plan} y \code{contactos}; antes de alimentar
\code{encuestar::construir_diseno_capas()} pásalo por \code{\link[=plan_para_capas]{plan_para_capas()}}.
}
\details{
Es el diseño de la Etapa I de los estudios por conglomerados censales
(p. ej. el espejo de la nota metodológica de Enkoll para Edomex 2026:
AGEBs con PPT). La Etapa II (manzanas dentro de cada AGEB sorteado) se
resuelve con \code{\link[=seleccionar_manzanas]{seleccionar_manzanas()}}; la Etapa III (viviendas por
manzana, sistemático con arranque aleatorio) es operación de campo y
queda dimensionada por \code{n_plan} y \code{contactos}.
}
